# Handoff

## Caregos editorial-hold automation repair (2026-08-30 12:33 KST)

- User goal: audit and optimize all dashboard sites one at a time using current first-party evidence, cost-routed multi-agent review, rollback, and live verification.
- Exact current state: Caregos remains an intentional editorial and AdSense hold. This bounded change converts the held-preview workflow from an automatic five-hour loop to an explicit manual action with fail-closed verification. Public indexability and content remain unchanged.
- Current source: GitHub `lsk7209/yoyangwon`, branch `main`, pre-change remote SHA `6cecb72bf40fef141b7fda690de16b6f00ec718e`. The original `D:\web\yoyangwon` checkout is 199 commits behind and has pre-existing untracked `scripts/__pycache__/`; it was preserved. Work is isolated in `output/claude-seo-fleet/caregos-remote-main`.
- Fresh dashboard: snapshot `2026-08-30T00:32:19.282Z`; GA4 29 users/29 sessions/18 views over 30 days; GSC 0 clicks/14 impressions/position 31.07; sitemap row 113 submitted, one child error, pending. A new 111-site visibility audit generated at `2026-08-30T03:25:38.280Z` correctly classifies Caregos as T0 intentional `editorial_hold`.
- Public proof before this change: home/About/Privacy are HTTP 200, self or configured canonical, one H1, `noindex`, and ad-free; unique missing path is real HTTP 404, `noindex`, and ad-free; robots is HTTP 200 with `Disallow: /`; sitemap is HTTP 200 with zero locations; RSS is HTTP 200 with zero items. The Git-connected Vercel status for pre-change SHA `6cecb72` was success.
- Confirmed automation defect: the scheduled workflow ran roughly every five hours while the hold was active. Every run regenerated held material, updated empty RSS metadata, committed, pushed, and triggered a Git-connected Vercel deployment without publishing any sitemap/RSS item or indexable page. Twenty recent scheduled runs and 198 identically titled automation commits among the 199-commit local/remote gap confirm the repeated loop.
- Prepared repair: `.github/workflows/release-due-posts.yml` is manual-only; exact `REFRESH_HELD_PREVIEW` confirmation is required; concurrency and a 15-minute bound are present; fail-open `always()` mutations are removed; and a fail-closed Python verifier checks all HTML plus required robots/sitemap/RSS artifacts before any commit. The workflow invocation, unused GSC dependency installation, and credential references were removed; `scripts/gsc_submit_sitemap.py` and historical documentation remain but are not invoked.
- Added regressions: `scripts/audit-editorial-hold-workflow.mjs` asserts the workflow safety/no-submission contracts, while `scripts/test_apply_adsense_review_hold.py` covers valid hold output plus missing, malformed, indexable, monetized, and discovery-bearing failure cases.
- Local validation: workflow regression 12/12, YAML parse, three review-hold unit tests, fail-closed verify-only execution, article-quality audit (103 posts plus 112 held), 100-row bulk tracking, CMS data validation (8 facilities), hosting-cost audit, and `git diff --check` pass. `validate_public_seo.py` fails because it expects 101 due posts in sitemap/RSS; that validator targets a released/indexable build and is intentionally incompatible with the current empty hold output.
- Dashboard hygiene: the unrelated static Caregos record's stale WordPress REST endpoint was removed in `D:\web\multi-dashboard\scripts\setup\sites.yaml`; 111-site config load and 10 focused fleet-audit tests pass.
- Changed files: workflow, workflow regression, fail-closed hold applicator/verifier and its unit tests, Python-cache ignore rule, README, and this handoff in the isolated Caregos clone; one Caregos metadata line plus verifier/audit/Harness documentation in the dashboard repo. No page, post, dataset, robots, sitemap, RSS, ad, analytics, GSC, DNS, or Vercel account setting changed.
- Side effects/rollback: revert this bounded change to restore the five-hour schedule. That rollback would reintroduce repeated held-preview commits/deployments and should be used only for a confirmed operations regression.
- Deliberately not run or sent: no workflow dispatch, GSC/IndexNow submission, sitemap removal/resubmission, editorial approval, publication, noindex/robots removal, ad enablement, Vercel CLI/API mutation, DNS change, data backfill, or content rewrite.
- Single resume gate: keep the manual workflow undispatched until separate editorial approval exists; before any future dispatch, revalidate the corpus and confirm the public hold is still intended.
